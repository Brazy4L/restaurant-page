const menu = () => {
    let wrapper = document.getElementById('wrapper');

    let content = document.createElement('div');
    content.id = 'content-menu';
    wrapper.appendChild(content);
}

export default menu;